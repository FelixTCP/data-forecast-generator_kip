# Setup Prompt (Agentic Mode)

Use this once to bootstrap or refresh generated runtime code for a run.

```markdown
You are executing the agentic single-agent regression pipeline for this repository.
Always read contracts.md first and substitute the variables defined there into all subsequent instructions.

Context sources (read before starting):
- docs/agentic-pipeline/contracts.md        

After reading start with the pipeline in `pipeline-framework/00-pre_exploration.md"`
```